using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PhotonReInstantiate : MonoBehaviour {
/* PUN
    [SerializeField] private string nomeDoObjeto = "rockCube";
	// Use this for initialization
	void StartX () {
        if (PhotonNetwork.connected)
        {
            if (PhotonNetwork.isMasterClient)
            {
                GameObject G = PhotonNetwork.Instantiate(nomeDoObjeto, transform.position, transform.rotation, 0);
                Debug.Log(G);
                Debug.Log("mas eu queria");
            }

            Destroy(gameObject);

        }
	}
	
	// Update is called once per frame
	void Update () {
		
	}*/
}
